import {Inter, Lalezar} from "next/font/google";

export const interRegular = Inter({subsets: ["latin"]});

export const interBold = Inter({subsets: ["latin"], weight: "700"});

export const lalezar = Lalezar({subsets: ["latin"], weight: "400"});
